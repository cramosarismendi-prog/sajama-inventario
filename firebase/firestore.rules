
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DESARROLLO LOCAL: permite todo en el emulador.
    // Se reemplaza por reglas seguras antes de subir a producción.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}